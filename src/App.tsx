import { Button } from '@/components/ui/button';

const App = () => {
  return (
    <div>
      <Button variant={'destructive'} className="m-10">
        Click me
      </Button>
    </div>
  );
};

export default App;
